# Agent Instructions

## 连续性维护

- 当项目内容发生重大变更、会话交接或用户要求同步上下文时，调用 `handoff-maintainer` 更新 `HANDOFF.md`。
- 更新前优先查看 git 记录；不足时再检索项目文件。
- 只修改受影响的章节，遵循 skill 中的更新矩阵。
- 决策记录使用 ADR-lite 格式，总数不超过 10 条。
- `待处理事项` 保持最多 3 条未完成可见；超过时清理已完成项。
- 每次更新后刷新 `HANDOFF.md` 底部的 `fresh-meta` 块。
- 最终回复列出未完成待办，并提示用户可回复“完成、删除或改写”。
