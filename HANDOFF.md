# Handoff — 项目连续性记录

> 新会话冷启动先读本文件。项目内容发生重大变化后，同步维护当前现状、待办和决策。

## 项目心智模型

- Camera Float 是 Electron + Vue 的桌面工具，现有核心由悬浮摄像头画面、独立摄像头工具栏和桌面标注会话组成。
- 摄像头窗口与标注窗口独立；标注会话由屏幕选择器、全屏透明覆盖层和浮动工具栏组成。

## 设计原则

- 保持摄像头既有接口和行为，桌面能力使用独立窗口与 IPC 边界。
- 桌面标注只覆盖显示内容，不修改底层应用；非绘制状态通过鼠标穿透继续显示标记。
- 当前使用原生 Canvas，不为绘图功能新增第三方依赖。

## 当前现状与限制

- 摄像头窗口支持滚轮和工具栏滑杆调整大小；拖动窗口期间会屏蔽滚轮/触控板缩放干扰。
- 摄像头窗口形态支持圆形、方形和超椭圆；工具栏中的“角”使用四次超椭圆轮廓，与方形保持明显区分。
- 摄像头工具栏是固定 `440×82px` 的两行跟随窗口，不随摄像头缩放；仅在鼠标位于摄像头或工具栏区域时显示。
- 摄像头工具栏的“标注”强调按钮可打开或关闭标注会话，并同步选择器、标注层和外部关闭状态。
- 单屏直接开始标注；多屏每次新建会话先显示屏幕信息和缩略图供用户选择。
- 已支持画笔、荧光笔、直线、箭头、矩形、椭圆、文字、橡皮擦、颜色、线宽、撤销、重做和清屏。
- 标注颜色使用不遮挡原色的外圈选中态；文字工具点击画布后就地输入，按 Enter 提交。
- 标注工具栏独立于绘制层并使用更高置顶层级；绘制层可隐藏后恢复且保留笔迹和历史。
- `Esc` 进入鼠标穿透，`Command/Ctrl+Alt+X` 可全局关闭标注。
- 标注只保存在当前会话内；切换鼠标穿透会保留，关闭会话会丢弃。
- macOS 未授予屏幕录制权限时无法取得屏幕缩略图，选择器会显示占位状态，但仍可按屏幕信息选择。
- Release 工作流由各平台直接上传安装包到草稿 Release，全部构建成功后再发布，不依赖 GitHub Actions Artifact 存储额度。
- App 运行时图标与 macOS、Windows、Linux 打包图标统一由 `camera-icon.png` 源图生成。
- 2026-07-15 已在单屏 macOS 环境实测通过摄像头黑边、拖动缩放干扰和同屏标注黑屏修复；多屏和 Windows/Linux 实机行为尚未验证。
- 2026-07-17 已将 Windows 摄像头拖动坐标统一为主进程 DIP 坐标，并在拖动期间锁定起始宽高，避免 DPI 调整放大窗口；本地构建通过，尚待 Windows 实机验证。
- 2026-07-19 已重置 Git 历史为单一根提交，并删除此前的 GitHub Release 与构建标签；后续发布从新的提交历史重新开始。
- Windows 不支持 macOS 式多级 topmost 层级；绘制层聚焦后改为显式恢复摄像头与工具栏顺序，尚待 Windows 安装包实测。

## 演进方向

- 先完成多屏与各目标平台验收，再评估对象选择、聚光灯、放大镜或截图导出。

## 待处理事项

- [ ] 在双屏环境验证缩略图映射、不同缩放比例、负坐标屏幕和屏幕热插拔。
- [ ] 在 Windows 高 DPI 环境回归摄像头拖动尺寸锁定与显式窗口排序，并继续验证 Windows/Linux 的鼠标穿透和屏幕缩略图行为。
- [ ] 用真实鼠标回归确认标注层开启后工具栏始终可见、可点击并能退出。

## 决策记录

### DR-20260714-01: 桌面标注采用独立透明覆盖层

- **背景**: 需要在桌面或任意应用上方实时涂写，同时保留现有悬浮摄像头功能。
- **决策**: 使用独立全屏透明标注层和更高层级的独立工具栏，多屏新会话先选择目标显示器。
- **原因**: 不侵入底层应用，能复用 Electron 置顶与鼠标穿透能力，并隔离摄像头窗口风险。
- **影响**: 标注状态按会话保存在渲染进程；跨屏标注需要为每块屏幕单独创建覆盖层后再扩展。
- **日期**: 2026-07-14。

### DR-20260714-02: 摄像头工具栏与画面窗口分离

- **背景**: 工具栏嵌在摄像头窗口内时会随画面缩放而换行和变形。
- **决策**: 摄像头画面和固定两行工具栏使用两个独立窗口，由主进程统一同步位置、悬停显隐、置顶和尺寸状态。
- **原因**: 工具控件布局不再受摄像头大小影响，同时保留现有画面缩放规则。
- **影响**: 摄像头设置通过 BroadcastChannel 在两个渲染窗口间同步；窗口生命周期必须成对管理。
- **日期**: 2026-07-14。

### DR-20260719-01: 重置项目 Git 历史

- **背景**: 项目已进入稳定阶段，旧提交记录和按提交哈希生成的构建 Release 不再具有保留价值。
- **决策**: 保留当前完整工作区内容，重建为单一根提交，并删除旧 Release 与构建标签。
- **原因**: 让仓库以当前稳定代码作为可追溯起点，避免历史提交中的个人真实姓名继续暴露。
- **影响**: 旧提交、Release 和标签不再可通过仓库引用访问；后续提交使用 GitHub 账号 `MarkTina` 的隐私邮箱。
- **日期**: 2026-07-19。

<!-- fresh-meta
last-updated: 2026-07-19
trigger-reason: 重置 Git 历史并移除旧发布记录
updated-by: handoff-maintainer
next-review: 完成新的版本化 Release 工作流后
-->
